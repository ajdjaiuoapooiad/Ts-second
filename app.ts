const person = {
    name: "Max",
    age: 30,
    hobbys: ['develop','read','excise','cooking','sports'],
}

console.log(person.name);
for(const hobby of person.hobbys){
    console.log(hobby);
    
}
